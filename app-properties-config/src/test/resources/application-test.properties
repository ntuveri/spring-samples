# Docs https://docs.spring.io/spring-boot/docs/current/reference/html/spring-boot-features.html#boot-features-external-config

# If not specified the active profile is default
# spring.profiles.active=default

app.name=Test resource application name (test profile)
app.test.specific=Test resource additional application value (test profile)